import './style.css';
import QuickWallHeroApp from './app';

// Wait for the DOM to be ready
document.addEventListener('DOMContentLoaded', () => {
  // Initialize the app
  const app = new QuickWallHeroApp();
});
